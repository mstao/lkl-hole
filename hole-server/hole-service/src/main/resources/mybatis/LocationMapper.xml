<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper  
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">  

<mapper namespace="com.lkl.hole.service.dao.LocationDao">

    <insert id="insert">
        INSERT INTO hole_location(bid, location_name, latitude, longitude)
        VALUES(#{bid}, #{locationName}, #{latitude}, #{longitude})
    </insert>

</mapper>
